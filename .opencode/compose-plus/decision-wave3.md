# DECISION Wave-3: недостатки аналогов → в AMSUR (15.09.2026, автономно)

> Синтез — оркестратор (запрет nemotron в силе). Вход: research-треки
> (аналоги + конкурсы/школы РБ), S1 Ground-Truth, чтение домена/UI.
> Принцип пользователя: всё гибко, но обычный пользователь не должен
> запутаться и нажать не то → дефолты из коробки + экспертное за флажками +
> запреты с объяснением вместо молчаливого «нельзя».

## CHECKLIST
1. Отклонено: Splits v2 / DnD+Undo / 6-й день+WeekType / справочник-как-данные /
   замены / веб-версия — каждое отдельный EPIC (мотив в плане-wave2, R-W2-4).
2. Противоречий ролей нет (ролей не запускали); конфликт «гибко vs просто»
   разрешён правилом D-34: дефолт работает из коробки, каждая настройка —
   вкл/выкл+параметры, опасное — через ConfirmDangerous.
3. OWN: SanPinChecker как отчёт (не gate — не ломает INV-01); PE-флаг из
   официального названия при импорте; CSV как мост к РИОС.
4. Риски: caps СанПиН — NEEDS-CHECK (текст №206 не прочитан целиком);
   реформа-2019 (чередование отменено) чинится в доках сейчас.
5. UI↔арх: чекер — чистый ридер (problem+placements), UI только показывает;
   кнопка «Проверить СанПиН» в ScheduleWindow рядом с правкой.
6. Логика↔UX: каждая находка чекера — текст «кто/где/что» + NEEDS-CHECK
   где норма не сверена; ошибок-«нельзя» без причины нет.
7. Трассировка: дыра1 (нормы РБ) → P-SANPIN-*; дыра2 (объяснение) → уже есть,
   +строки чекера; дыра3 (0₽+открыто+рус+офлайн) → есть; подгруппы/смены →
   было; правка с контролем → было + чекер; печать → было (Excel/HTML) + CSV;
   простота → P-GUIDE.

## Пакеты (ворота: build + затронутые тесты + WORK_LOG)
P-SANPIN-DOC, P-PE-FLAG, P-SANPIN-CHECK, P-SANPIN-UI, P-CSV, P-DAYOFF, P-GUIDE.
