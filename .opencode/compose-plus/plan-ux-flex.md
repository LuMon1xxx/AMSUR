# План UX+Flex (по промту ученика 15.09.2026) — рабочий файл оркестратора

Ответы ученика: 6-дневка = глобально + по классам; дизайн на stitch-базе;
тур по 4 окнам; сначала проводка, потом дизайн.

## S1-АУДИТ (факты, проверено кодом 15.09)

### Окна (12 xaml): Main / Generate / Schedule / SchoolData / Settings / Export /
### Help / QualityDetails / DangerDialog+Confirm / LoadRow / TrajectoryChart
- Стили: App.xaml 34КБ, токены совпадают с handoff (WORK_LOG 14.09). База stitch есть.
- HelpWindow — статический текст, тура нет. DangerDialog+ConfirmDangerAsync + «не спрашивать» — есть, работает для весов.
- Undo/Redo/DnD/Pin в WPF — НЕТ вообще (grep пуст). Правка — диалоги/комбо.
- ExportWindow: нет черновика, нет персональных видов (только классы-общее превью).
- SettingsWindow: слайдеры 0..4 уже современные; перегрузки/6-дневки нет.

### Гибкость: что уже свободно, где хардкоды
- СВОБОДНО: дни/слоты — произвольные числа при импорте (SchoolDataWindow: любых >0);
  кэпы учителей/классов — поля модели; веса/профили — данные; перегрузка — движок+персист (D-39).
- ХАРДКОДЫ/ПРОБЕЛЫ (чинить):
  1. `ScheduleWindow` матрица только при Days≤6/Slots≤9 (строка 171) — display-лимит; при 6-дневке × 2 смены (6×14) матрица не показывается. Поднять/переписать.
  2. Нет дней-на-класс: ClassConfigRow (Grade/StudentCount) без DaysPerWeek; глобального переключателя 5/6 тоже нет. Движок classSlots умеет слоты-на-класс, дни-на-класс — проверить ProblemBuilder allowedDays (investigate).
  3. DayNames 7 шт (Пн..Вс) — для 6-дневки ок, но подписи «Сб» как учебного дня нигде не объяснены.
  4. Нет UI: перегрузка (движок готов), черновик (экспортёр готов), персональные виды (скрипт).
  5. Нет предупреждений: смена дней 5→6, слотов, снятие ONLY-кабинетов, выключение UseOwnRooms — без DangerConfirm (веса — с ним, остальное — нет).
  6. Настройки слотов/смен per-class в SchoolDataWindow: 8 табов, дней-на-класс нет.
  7. Классный час: день жёстко четверг? CommonLesson DayIndex — проверить UI (SchoolDataWindow вкладка?).
  8. PDF-одноклик отсутствует (осознанно, D5) — HTML-путь объяснён.

### Тур-обучение: с нуля. Overlay-подсветка + шаги + «нажми» — hand-rolled
### (сторонних WPF-тур-библиотек в проекте нет, тащить зависимость ради тура — нет).
### Окна тура: Main, Generate, Schedule, Settings.

## Пакеты (порядок: проводка → гибкость → дизайн → тур)

### P-A ПРОВОДКА (сейчас)
- A1. SettingsWindow: секция «Нагрузка и пределы»: тумблер перегрузки + cap 7..14
  (через ConfirmDangerousAsync, персист SqliteFlexStore — готов) + сноска «кто поднят».
  + Тест: пресет roundtrip через store (есть) + UI-конструкция (STA).
- A2. ExportWindow: кнопка «Выгрузить черновик» (активна когда нет активного ИЛИ
  всегда с пояснением) + вид «Учителя/Кабинеты» уже есть; добавить персональный вид:
  ComboBox учителя → превью его сетки → выгрузка его листа. + Тесты.
- A3. ScheduleWindow: персональные вкладки уже есть (Класс/Учитель/Кабинет)? Проверить;
  если нет — добавить переключатель вида (дешёво, данные те же).

### P-B ГИБКОСТЬ «ВСЁ НАСТРАИВАЕТСЯ»
- B1. Дни: глобальный DaysCount (уже есть) + DaysPerWeek per-class (ClassConfigRow,
  store-миграция, SchoolDataWindow-колонка, импортёр → classSlots/days в ProblemInput).
  Investigate: allowedDays per-class в ProblemBuilder.
- B2. Слоты/смены per-class: уже classSlots; добавить UI-редактор смены класса
  (1-я/2-я/другая) — проверить наличие; если нет — комбо в SchoolDataWindow.
- B3. Кэпы: MaxLessonsPerDay класса/учителя — редакторы с DangerConfirm при росте
  сверх СанПиН-норм (6/7/6). Правило: рост = предупреждение, снижение = молча.
- B4. День классного часа: вынести DayIndex в настройки (сейчас? проверить) + слот.
- B5. Матрицу ScheduleWindow поднять до 7 дней / 14 слотов (виртуализация не нужна).
- B6. Предупреждения: 5→6 дней, слоты, ONLY, UseOwnRooms, выкл. валидации — все через
  ConfirmDangerousAsync с человеческими последствиями.

### P-C ДИЗАЙН (stitch-база)
- C1. Токены/контролы: слайдеры, кнопки, тумблеры, бейджи — единый современный вид
  (Fluent-оттенок поверх stitch: скругления, hover, фокус).
- C2. Заметные CTA: «Сгенерировать» hero-кнопка на Main + F5-шильд; «Принять лучший»
  в Generate; «Выгрузить» в Schedule/Export — крупные, с иконками и подсказками.
- C3. Пустые/ошибочные состояния человеческим языком (уже частично есть — добить).
- Проверка: WpfShell-конструкция + скриншоты до/после через mimo (1 раунд).

### P-D ТУР-ОБУЧЕНИЕ (4 окна)
- D1. Инфра: TourService (шаги: целевой элемент → подпись → «Далее/Пропустить»,
  подсветка-dimmer overlay, клик-просьба с проверкой). Без зависимостей.
- D2. Контент: Main (5 шагов пути), Generate (режим→старт→Top5→принять),
  Schedule (фильтры→правка→выгрузка), Settings (профиль→слайдеры→опасное).
- D3. Точка входа: кнопка «?» + «Обучение» в Help + автопоказ при первом старте
  (флаг в AppSettings). + Тесты STA (шаги строятся, overlay не падает).

## Правила по ходу
- Каждое ослабление нормы = DangerConfirm + видимая метка (не молча).
- Ни один Accepted без Hard==0 (INV-01) — черновик помечен, не Accepted.
- Тесты рядом с кодом; сьют зелёный после каждого пакета.
- Коммит/пуш — по команде ученика.

## Progress 15.09 (noch): P-A GOTOV (A1+A2+A3-chastichno: personalnye vidy v Export, ne v Schedule). Ssuit 299+1/0.

## Progress: P-C GOTOV (D-44). Ssuit nije.
