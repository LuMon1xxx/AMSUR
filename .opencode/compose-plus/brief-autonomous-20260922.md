# Бриф автономной итерации (22.09.2026, ~1 час)

## Задача пользователя
Улучшить проект автономно: сделать более гибким и удобным, но оставить простым
для новичка за ПК (завуч). Сохранить версию для отката, всё записывать, искать
инфу при нужде, запускать тесты, делать скрины.

## Точка отката
- Ветка `backup-autonomous-20260922` + тег `pre-improve-20260922` на HEAD (e9a5ec5).
- Грязные изменения пользователя НЕ тронуты; их дифф — `.opencode/compose-plus/pre-improve-dirty.diff`.
- Откат: `git checkout backup-autonomous-20260922` (+ при нужде применить diff обратно).

## Наблюдение (S1, сам)
Проект зрелый (EPIC-K, 301 тест). Главный барьер новичка — ввод данных:
шаблон → заполнить 50+ строк → загрузить. Демо-набор существует только в тестах
(`DemoSchoolTests.DemoRows`), в продукт не проброшен.

## Решение (S5-lite, SHORT: 5 файлов, откат за минуты)
Пакет A «Демо в 1 клик»:
1. `Amsur.Application/DemoSchoolData.cs` — демо-набор как данные продукта
   (единственный источник; `DemoSchoolTests.DemoRows()` делегирует ему).
2. `AppSession.ImportDemoAsync()` (source="demo", дни 5, слоты 7).
3. Кнопка «Попробовать демо» на дашборде (видна только пока нет данных)
   + кнопка «Загрузить демо» в «Школа и данные».
4. Тест `DemoDataTests` (проблема строится, импорт в сессию ок).
5. `ПРОСТАЯ_ИНСТРУКЦИЯ_ЗАВУЧУ.md` — 1 страница: 5 шагов крупно.

НЕ делаю: правки движка/весов/валидатора (freeze), редизайн, удаление чужих файлов
(ScratchGen.cs, данные пилота — руки прочь), скриншоты WPF-окон (в этой среде нет
дисплея для достоверного GUI-скрина — честно фиксирую как ограничение вместо
фейковых картинок).

## Критерии приёмки
1. `dotnet build src/Amsur.slnx` — 0 ошибок.
2. Новые/затронутые тесты зелёные: `DemoDataTests`, `DemoSchool_HasExpectedShape`,
   STA shell-тесты окон.
3. Кнопки демо видны только когда уместны; после загрузки демо дашборд показывает
   «Демо-данные», генерация доступна.
4. WORK_LOG + финальный отчёт пользователю: сделано / проверено / не решено.
