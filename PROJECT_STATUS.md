# АМСУР — статус проекта, тесты и настройка (единый файл, 13.09.2026)

> Статус: **EPIC-K готов** (FULL UI — продукт: dashboard, режимы, настройки, рейтинг).
> Полный сьют (20.09.2026, день): **301 пройдено + 1 skipped, 0 failed за 2м29с**
> (карантин D-35; +2 PhotoSchool: реальные фото 5–11; было 299+1). V1 (`D:\LuMon1x\Pr_Auto`) не тронут.
> Технические детали — `MASTER_PLAN.md`, `DECISIONS.md`, `BENCHMARKS.md`.

---

## 1. Что такое АМСУР

Автоматическое составление школьного расписания для типовой белорусской школы:
ввод нагрузки → «Сгенерировать» → Top-5 вариантов с объяснением → ручная правка → Excel.
Стек: .NET 10, WPF, SQLite (без EF), OR-Tools CP-SAT + собственный локальный поиск.

Принцип: **максимально гибко, максимально просто**.
Обычный завуч ничего не настраивает (профиль «Стандарт»). Эксперт настраивает
глубоко — через интерфейс, без кода.

---

## 2. Что уже реализовано

### Движок расписания
- Greedy-старт (1196/1196 за ~0.1–0.15с) → CompactRepair (компактность ученика) →
  LocalSearch VND (single + swap) → polish-циклы → LNS-страховка → GradeOneBalance →
  **FullValidator-gate** (при Hard>0 результат отклоняется, файл не пишется).
- CP-SAT Phase B пропускается на школах ≥300 уроков (там он даёт 0 улучшений),
  на малых работает как раньше.
- Детерминизм: тот же seed = тот же результат (покрыто тестом).

### Качество: split teacher-gap (новое в EPIC-J)
Окна учителей разделены на две видимые категории:
- **Окна внутри смены** (урок 1 → окно → урок 3), вес 10 — основной штраф;
- **Перерывы между сменами** (утро + вечер), вес 2 — естественны для двухсменки,
  дешевле, но не бесплатны и видны отдельно.
- Ученики: **0 окон, старт не позже 2-го урока** — строгие правила, одинаковые
  для всех профилей, торговаться весами нельзя.

### Профили качества (данные, не код)
| Профиль | Смысл |
|---|---|
| Стандарт (по умолчанию) | Как обычно; ordinary 10 / cross 2 |
| Удобно ученикам | Окна учителей дешевле (8/1), ученик-приоритет в рамках строгих правил |
| Удобно учителям | Окна учителей дороже (20/5) |
| Наш профиль (CUSTOM) | Снимок ручных настроек школы, хранится в SQLite |

### Приложение (WPF)
- MainWindow-dashboard (по референсу primer): карточки школы/профиля, CTA-баннер
  «Сгенерировать расписание», мини-карточки данных/шаблона/профиля/последнего расписания,
  режимы генерации, панель качества, совет, быстрый старт, inline-ошибки импорта.
- Режимы: **Быстро** (3с) / **Стандарт** (12с, по умолчанию) / **Максимум** (30с, 5 seed) /
  **Экспертная** (полные настройки + генерация).
- GenerateWindow: стадии, Стоп без потери лучшего, Top-5 карточек, блок
  «Почему такая оценка», строка «Профиль · Режим», график улучшения.
- Качество человеческим языком: **Отличное / Хорошее / Требует внимания** + топ-3
  «что можно улучшить» + окно «Почему так» (вместо голого Soft).
- SettingsWindow: пресеты + слайдеры «Не важно … Очень важно» с подсказками и бейджами
  СТРОГОЕ/ПОЖЕЛАНИЕ + предметы/классы/учителя + эксперт (числа, JSON, «Сохранить „Моя школа“»
  с восстановлением при старте).
- ScheduleWindow: фильтры Класс/Учитель, строка качества, ручная правка с превью
  (можно / можно с ухудшением / запрещено + причина), выгрузка в Excel (и с dashboard).

### Данные типовой школы
- 40 классов (1–9 АБВГ, 10–11 АБ), 100 учителей, 40 кабинетов, 5 дней × 2 смены.
- **1–4 классы ведёт один классный учитель** (физкультура и музыка — специалисты).
- Планы по нормам 5-дневки 21/23/29/30/32/33/34; пример: `RealSchool_Schedule_EPICJ.xlsx`
  (40 классов, 0 окон, 0 поздних стартов — проверено независимым аудитом).

### Пилот СШ №8: реальные фото 5–11 (20.09.2026, `данные/`)
- 6 фото висящего расписания → `НашаШкола_5-11_нагрузка.xlsx` (24 класса,
  396 строк, 769 ч/нед; генератор `build_load_5-11.py`; маппинг `МАППИНГ_5-11.md`).
- Факты по фото: 2-я смена только 6–7-е; сплит ин.яза есть и в 5-х; ОБЖ в 5-х есть;
  музыки в 5–11 нет; учителя на фото отсутствуют (плейсхолдеры, сверить с завучем).
- Движок на фото-данных: greedy 878/884, repair 21→10 окон, pipeline ×3 seed 5/7/5.
  Зазоры под октябрь: смена в Load (G1), пин классного часа (G2), Splits v2 (G3).

---

## 3. Последние тесты и замеры

### Сьют: 184/184 → **188/188**
- 163 базовых (валидатор, solver, subgroups, паритет, персистентность, обмен, UI-shell, E2E-цикл).
- +15 split/parity/profiles (`TeacherGapSplitTests`: таблица ordinary/cross,
  backward-совместимость, паритет индекс==full на 3 наборах весов, профили, шкала).
- +5 персистентность/профили (`QualityProfileTests`: roundtrip CUSTOM, single-active,
  отказ невалидных весов до записи, подсказки, проброс профиля в solver/оркестратор).
- +1 модель началки (`PrimaryClasses_SingleClassTeacher`).
- +3 UX-логики (`QualityUxTests`: режимы, рейтинг, редактор) +1 STA всех окон.
- STA-smoke: 6 окон конструируются; exe запускается (12с headless без краша).

### Сьют wave-2/3 (15.09.2026, ночь): **287/288 за ~2м41с**
- +Flex P1–P5 (SchoolFlex, RoomPolicy, lanes, ONLY, R7 fail-loud, DangerStrict),
  HeavyFlex, MidSchoolTight, DemoSchool, WpfShell-регрессия BasedOn.
- 1 KNOWN-FAIL: `MidSchoolTight_StandardRun_Feasible` (новый тест, 2/2 repro:
  tight-фикстура 994 occ, greedy 986/994, solver выходит ~4с/seed без feasible;
  чинить вслепую запрещено — карантин, D-35). Остальные 287 зелёные,
  в т.ч. новые HtmlExportTests (4), SubjectAliasTests (6), SanPinCheckerTests (5),
  CsvExportTests (3), DayOffTests (11).

### Ключевые замеры (RealSchool 1196, seed 11)
- Аудит массы: было 1389 юнитов teacher-gap = 395 ordinary + **994 cross (72%)**.
- Матрица веса cross (бюджет 20с, везде Feasible/Hard=0/student 0/0, ~12с):

| wCross | ordinary | cross | subj | итог (новый) | итог (старой метрикой) |
|---|---|---|---|---|---|
| 0 | 193 | 772 | 28 | 2350 | 10070 |
| 1 | 176 | 619 | 29 | 2814 | 8385 |
| **2 (дефолт)** | **200** | **477** | **28** | **3374** | **7190** |
| 5 | 243 | 336 | 30 | 4560 | 6240 |
| 10 (старая динамика) | 310 | 333 | 31 | 6895 | 6895 |

- Эффект решения (w10→w2, всё остальное равно): ordinary **310→200 (−35%)**,
  cross 333→477 (+43%), student 0/0, Hard 0. Старая метрика 6895→7190 (+4% —
  осознанная цена честного разделения).
- Gate (бюджет 10с): Feasible, Hard=0, soft 3549, 6.3с.
- Seed-stability: spread 5.5% (3075–3249) — узко.
- Важно: смена веса wearable ломала сходимость (REJECT); починено targeted-стартом
  поиска без новых движков. ILS/tabu/chain не понадобились и не вводились.

---

## 4. Как пользователь настраивает приложение (сейчас)

1. Открыть программу → скачать шаблон Excel → заполнить нагрузку → загрузить
   (ошибки покажут списком «что исправить» прямо на главном экране).
2. Оставить профиль «Стандарт» и режим «Стандарт» (или выбрать свои).
3. Нажать «Сгенерировать расписание».
4. Сравнить Top-5: оценка словами (Отличное/Хорошее), топ-3 улучшений, «почему так».
5. Принять лучший → при желании поправить вручную (запрещённое объяснит причину) →
   выгрузить в Excel (из расписания или с главного экрана).

Обычному завучу этого достаточно. НИКАКИЕ числа крутить не нужно.

---

## 5. Что вообще настраивается (и будет настраиваться)

Строгое vs пожелание: **строгое** нарушить нельзя (валидатор отклонит),
**пожелание** можно — но снизит оценку варианта.

| Настройка | Тип | Дефолт | Что будет при усилении |
|---|---|---|---|
| Окна у учеников | Строгое | 100 | Меньше окон у детей, но учителям неудобнее |
| Позднее начало дня | Строгое | 100 | Меньше дней «со 2-го», но нагрузку уложить сложнее |
| Окна у учителей | Пожелание | 10 | Плотнее день учителей, возможны окна у детей |
| Перерывы между сменами | Пожелание | 2 | Меньше разрывных дней, но больше окон внутри смен |
| Тяжёлые уроки на краю дня | Пожелание | 20 | Тяжёлые уйдут в середину дня |
| Повторы предмета за день | Пожелание | 15 | Меньше сдвоенных, но расписанию теснее |
| Кабинеты | Пожелание | 5 | Меньше перебежек, но больше окон |
| Нормы (СанПиН-блок) | Требует сверки | выкл. | Не трогать вслепую — сначала сверить с нормами |

Сейчас: выбор профиля и режима на главном экране; полные настройки — кнопка
«Настроить»/«Изменить» (пресеты, слайдеры, предметы/классы/учителя, эксперт, сохранение
«Моя школа»). Следом не требуется — диалог уже полный; backlog: day-уровень доступности
учителей, doubles/adjacency-предпочтения (персист школьных данных ГОТОВ: SQLite + restore при старте, P3).

---

## 6. Как читать оценку варианта

- `Soft N` — суммарный штраф (меньше = лучше); рядом «0 жёстких нарушений».
- «Основное: …» — главный источник штрафа.
- Строки качества называют виновника: класс/учитель, день, уроки.
- «Перерыв между сменами (обычно неизбежен)» — не дефект школы, а цена двухсменки.
- Строки с «требует сверки с нормами» — непроверенные нормативы, не решение завуча.

---

## 7. Честные ограничения (не скрываем)

- «Лучшее найденное», никогда «оптимальное» — solver ограничен временем.
- СанПиН-веса **не сверены с НПА** (NEEDS-CHECK); нормативная корректность не заявляется.
- Таймаут ≠ «невозможно»: честные статусы «не найдено за время» / «остановлено, лучшее сохранено».
- Сдвоенные уроки не запрещены и не preferred по умолчанию; физкультура не привязана
  к краям дня — решает общая оптимизация, школа может настроить предпочтение.
- Полный диалог настроек — в backlog; движок/форматы к нему готовы.

---

## 8. Для разработчика (кратко)

- Слои: `Amsur.Domain` / `Scheduling.Core` / `Scheduling.OrTools` /
  `Application` / `Infrastructure` (raw SQLite) / `Wpf` / `Tests`.
- Ключевые файлы: `GapUtils.cs`, `QualitySettings.cs` (веса+профили+шкала),
  `RuleCatalog.cs` (v5), `SoftEvaluator.cs`, `SearchIndex.cs` (`LocalSearch.cs`
  targeted-старт), `QualityExplainer.cs` + `QualityHints.cs`, `QualityProfiles`
  (`SqliteQualityProfileStore.cs`), `RealSchoolStressTests.cs` (fixture, классные 1–4).
- Команды: `dotnet build src/Amsur.slnx`, `dotnet test src/Amsur.Tests` (~2 мин).
- Артефакты Compose+: `.opencode/compose-plus/` (baseline, critique, decision, plan, risks).
